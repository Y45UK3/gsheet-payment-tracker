# Server-only. Do NOT prefix these with NEXT_PUBLIC_.
IGE_APPS_SCRIPT_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
IGE_REGISTRATION_KEY=YOUR_REGISTRATION_KEY

# Optional demo fallback. Set to true only when presenting UI without the real backend.
IGE_DEMO_MODE=false
